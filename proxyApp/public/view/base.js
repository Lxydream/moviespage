
export default class { //登陆页面的视图
    constructor({
        el
    }) { //element
        this.el = el;
        // this.reu=null;
        // this.reu1=null;
        this.init();
    }
    init() {
        this.render();
        this.mounted();
        this.handle();
    }
    render() { //负责页面渲染的
   
    }
    mounted() { //负责挂载一些其他操作
        
    }
    handle() { //负责事件监听
       
    }
}